#include "main.h"
/**
 * set_string - func that sets a pointer@s value to char
 * @to: target address pointers value will be returned
 * @s: the address of source
 * Return: void
*/
void set_string(char **s, char *to)
{
	*s = to;
}
